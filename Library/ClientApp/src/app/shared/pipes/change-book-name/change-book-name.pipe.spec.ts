import { ChangeBookNamePipe } from './change-book-name.pipe';

describe('ChangeBookNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeBookNamePipe();
    expect(pipe).toBeTruthy();
  });
});
