import { ChangePropertyNamePipe } from './change-property-name.pipe';

describe('ChangePropertyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangePropertyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
